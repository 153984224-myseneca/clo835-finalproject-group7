apiVersion: v1
kind: Secret
metadata:
  name: aws-secret
type: Opaque
data: 
  aws_access_key_id: <aws_access_key_id> #(echo -n <aws_access_key_id> | base64)
  aws_secret_access_key: <aws_secret_access_key>
  aws_session_token: <aws_session_token>